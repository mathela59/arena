{% extends 'base.html.twig' %}

{% block title %}Hello {{  app.user.username }}!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="container">
        <h1>Hello {{ app.user.username }}!</h1>
        {% include('warrior/index.html.twig') %}
        <button class="btn btn-red"><a href="{{ path('app_combat_random') }}">Random Fight</a></button>
    </div>
{% endblock %}
