<nav class="navbar navbar-expand-sm bg-primary navbar-dark">
    <div class="container-fluid">
        <ul class="nav navbar-nav">
            <li class="nav-item"><a class="nav-link" href="{{ path('app_index') }}">ARENA</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_homepage') }}">Home</a></li>
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item"><a class="nav-link" href="{{ path('app_warrior_creation') }}">Create a warrior</a>
                </li>
            {% endif %}
        </ul>
        <ul class="nav navbar-nav navbar-right">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="dropdown nav-item">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Admin<span
                                class="navbar-toggler-icon"></span></a>
                    <ul class="dropdown-menu">
                        <li class="nav-item"><a class="link" href="{{ path('app_user_index') }}">Users</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_breed_index') }}">Breeds</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_slots_index') }}">Slots</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_traits_index') }}">Traits</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_fight_style_index') }}">Fight
                                Styles</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_skills_index') }}">Skills</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_sentence_index') }}">Sentences</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_items_index') }}">Items</a></li>
                        <li class="nav-item"><a class="link" href="{{ path('app_warrior_index') }}">Warriors</a></li>
                    </ul>
                </li>

            {% endif %}
            <li class="nav-item">{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>
