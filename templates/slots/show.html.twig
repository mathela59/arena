{% extends 'base.html.twig' %}

{% block title %}Slots{% endblock %}

{% block body %}
    <h1>Slots</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ slot.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ slot.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_slots_index') }}">back to list</a>

    <a href="{{ path('app_slots_edit', {'id': slot.id}) }}">edit</a>

    {{ include('slots/_delete_form.html.twig') }}
{% endblock %}
