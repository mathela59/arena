<form method="post" action="{{ path('app_fight_style_delete', {'id': fight_style.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fight_style.id) }}">
    <button class="btn">Delete</button>
</form>
